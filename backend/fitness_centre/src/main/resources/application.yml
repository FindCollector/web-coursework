spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitness_centre?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: "@Mayday21"
    driver-class-name: com.mysql.jdbc.Driver

    redis:
      host: localhost
      port: 6379
      # password: 如果 Redis 设置了密码，请在此处填写
      # database: 0   # 选择使用的数据库索引，默认为 0
      # timeout: 3000 # 连接超时时间（单位毫秒），可选

# 说明mapper在哪里（直接是使用默认值）
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml

server:
  port: 8080

recaptcha:
  secretKey: "6Lcq_e4qAAAAAEJYKkGw-zQ6CN74yjbiWByLBo6Y"
  threshold: 0.5
  API_KEY: "AIzaSyDkGZwzeqoVwtFNHfvKfSnEktdg16oqK3E"